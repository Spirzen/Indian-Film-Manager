@page
@model IndianFilmManager.Pages.Cinemas.IndexModel
@{
    ViewData["Title"] = "Управление фильмами";
}

<div class="container">
    <header class="text-center my-4">
        <h1 class="display-6">Список фильмов</h1>
    </header>

    <!-- Кнопка добавления -->
    <div class="mb-4">
        <a href="/Cinemas/AddEdit" class="btn btn-success">Добавить фильм</a>
    </div>

    <!-- Таблица фильмов -->
    <table class="table table-bordered table-hover">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Название фильма</th>
                <th>Год выпуска</th>
                <th>Рейтинг</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var cinema in Model.Cinemas)
            {
                <tr>
                    <td>@cinema.Id</td>
                    <td>@cinema.Name</td>
                    <td>@cinema.Year</td>
                    <td>@cinema.Score</td>
                    <td>
                        <a href="/Cinemas/AddEdit?id=@cinema.Id" class="btn btn-sm btn-warning">Редактировать</a>
                        <form method="post" asp-page-handler="Delete" style="display:inline;">
                            <input type="hidden" name="id" value="@cinema.Id" />
                            <button type="submit" class="btn btn-sm btn-danger">Удалить</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>