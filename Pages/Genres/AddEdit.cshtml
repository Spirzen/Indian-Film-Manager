@page
@model IndianFilmManager.Pages.Genres.AddEditModel
@{
    ViewData["Title"] = Model.IsEdit ? "Редактировать жанр" : "Добавить жанр";
}

<div class="ifm-page-header">
    <h1>@ViewData["Title"]</h1>
    <p class="subtitle">
        <a asp-page="/Genres/Index"><i class="bi bi-arrow-left"></i> Назад к списку</a>
    </p>
</div>

<div class="ifm-form-card">
    <form method="post">
        <input type="hidden" asp-for="Genre.Id" />
        <div class="mb-3">
            <label asp-for="Genre.Name" class="form-label">Название жанра</label>
            <input asp-for="Genre.Name" class="form-control" required maxlength="100" />
        </div>
        <div class="d-flex gap-2">
            <button type="submit" class="btn btn-ifm-primary">Сохранить</button>
            <a asp-page="/Genres/Index" class="btn btn-ifm-outline">Отмена</a>
        </div>
    </form>
</div>
